html
  include includes/head.pug

block content
  .main.container
    if (!user) 
      p Welcome! Please 
      a(href='/login') log in
    else
      p Hello, #{user.username}. View your 
      a(href='/profile') profile
